{
  "name": "sorare-bid-bot",
  "version": "1.0.0",
  "private": true,
  "description": "Superviseur et pilote d'un bot d'encheres automatiques sur le marche primaire de Sorare (proxy GraphQL officiel, dry-run inclus).",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "install:all": "npm install",
    "dev:backend": "npm --workspace backend run dev",
    "dev:frontend": "npm --workspace frontend run dev",
    "dev": "concurrently -n backend,frontend -c blue,magenta \"npm run dev:backend\" \"npm run dev:frontend\"",
    "build": "npm --workspace backend run build && npm --workspace frontend run build",
    "build:backend": "npm --workspace backend run build",
    "build:frontend": "npm --workspace frontend run build",
    "start": "npm --workspace backend run start",
    "test": "npm --workspace backend run test"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
